#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <assert.h>
#include <time.h>

int min(int x, int y);

int max(int x, int y);

unsigned absolute(int x);

int eucleidian(int x1, int y1, int x2, int y2); 

void error_message(char * message);